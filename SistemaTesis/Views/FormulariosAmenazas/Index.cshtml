@model IEnumerable<SistemaTesis.Models.FormularioAmenaza>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CatalogoAmenaza)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Formulario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CatalogoAmenaza.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Formulario.CuidoIndicadores)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FormularioAmenazaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FormularioAmenazaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FormularioAmenazaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
